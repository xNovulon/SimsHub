// The right panel: what is selected and what you can do with it right now.
// It lives in inspector/ (one file per card); this file keeps the old name, so every existing import still works.
export { renderInspector } from './inspector/index.js';
